package com.trulyao.bookie.lib

import androidx.compose.ui.unit.dp

val DEFAULT_VIEW_PADDING = 20.dp
val nameRegex = Regex("^[a-zA-Z]{3,}$")
val emailRegex = Regex("^[a-zA-Z0-9._-]+@bookie\\.ac\\.uk$")
